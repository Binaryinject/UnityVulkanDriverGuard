# Native proxy artifacts

The files in this directory are generated by `scripts/build-windows.ps1` and `scripts/build-linux.sh` and are consumed by the Unity build postprocessor.

For a release package, publish both architecture directories with the package:

- `Windows/x86_64/UnityPlayer.dll`
- `Linux/x86_64/UnityPlayer.so`

